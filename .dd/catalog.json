{
  "schema": 1,
  "comment": "Starting points for dd dep install NAME. URLs only: the default branch head is resolved at declaration time and recorded as a concrete commit in the project's cmake/dd-dependencies.json. Supply --git and --ref to pin something else.",
  "dependencies": {
    "platform-h": { "url": "https://github.com/ZacWalk/platform-h.git" },
    "spike-db": { "url": "https://github.com/ZacWalk/spike-db.git" }
  }
}
